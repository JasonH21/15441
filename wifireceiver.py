import numpy as np

# begin_zero_padding = Number of zeros appended at the beginning of the message
# in Level 4
# message = The message used to create WiFi packet
# length = the length of the message
nfft = 64
def WifiReceiver(output, level):
    begin_zero_padding,message,length = 0
    print(Interleave = np.reshape(np.transpose(np.reshape(np.arange(1, 2*nfft+1, 1),[-1,4])),[-1,]))
    return begin_zero_padding, message, length